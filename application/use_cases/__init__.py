"""Use-case boundary.

Current use cases are exposed through application services. Keep this package
for future command/query handlers used by the web dashboard, Telegram bot and
mobile API without changing presentation code imports.
"""
